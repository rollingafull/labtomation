---
# Ansible Galaxy requirements for Labtomation playbooks
collections:
  - name: ansible.posix
    version: ">=1.6.0"

  - name: community.general
    version: ">=10.0.0"

  - name: community.proxmox
    version: ">=1.3.0"

  - name: community.hashi_vault
    version: ">=6.0.0"
